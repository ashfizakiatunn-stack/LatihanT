// 1. pilih tombol berdasarkan ID-nya
const tombolsapa = document.getElementByld(`sapaButton`);
// 2. tambahkan `even listener` untuk aksi `click`
tombolsapa.addEventListener(`click`,functuion() {
  // 3. jalankan tombol ini ketika tombol di-klik
  alert(`halo! terimakasih sudah berkunjung!);
  }):
